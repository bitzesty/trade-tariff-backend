machine:
  ruby:
    version: 2.3.1
  services:
    - elasticsearch
    - redis
  environment:
    GOVUK_APP_DOMAIN: test
dependencies:
  post:
    - sudo service redis-server status || sudo service redis-server start
