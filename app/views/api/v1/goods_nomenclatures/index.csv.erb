<%= CSV.generate_line(['SID','Goods Nomenclature Item ID','Indents','Description','Product Line Suffix','Third Country Duty','Standard VAT','Zero VAT','Reduced VAT','Href'], {row_sep: ""}) %>
<%- @commodities.each do |c| -%>
<%= CSV.generate_line([
  c.goods_nomenclature_sid, 
  c.goods_nomenclature_item_id,
  c.number_indents,
  c.description,
  c.producline_suffix,
  api_path_builder(c)
], {row_sep: "\n"}).html_safe -%>
<%- end -%>
